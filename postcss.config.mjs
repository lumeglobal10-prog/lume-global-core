/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // 🎨 MOTOR DE ESTILOS LUME GLOBAL CORE
    tailwindcss: {},
    // 🛡️ COMPATIBILIDAD PERIMETRAL DE RENDERIZADO
    autoprefixer: {},
  },
};

export default config;
